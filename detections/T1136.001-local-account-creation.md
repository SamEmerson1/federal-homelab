# Local Account Creation

| Field | Value |
|---|---|
| **ATT&CK technique** | T1136.001 — Create Account: Local Account |
| **Tactic** | Persistence |
| **Log source** | Windows Security event log (WS01, DC01) |
| **Index** | `lab_win` |
| **Severity** | Medium |
| **Status** | Draft — pending atomic validation |
| **Author** | |
| **Created** | |
| **Validated** | |

## Hypothesis

An adversary who has gained execution on a workstation frequently creates a local account to
survive credential resets and to hold access independent of the compromised user. Windows records
local account creation as Security event 4720 on the machine where it happened. In a managed
environment, account creation should originate from the domain controller through a provisioning
process — a 4720 on a workstation is either a helpdesk technician working outside process or an
adversary, and both are worth a look.

The follow-on action, adding that account to a privileged group, generates 4732. Correlating the
two in a short window separates routine account creation from privilege establishment.

## Detection logic

```spl
index=lab_win source="WinEventLog:Security" EventCode=4720
| rename TargetUserName as new_account, SubjectUserName as created_by
| search NOT created_by IN ("*$", "SYSTEM")
| table _time, host, new_account, created_by, SubjectDomainName
| sort - _time
```

Correlated version — creation followed by privileged group membership within 10 minutes:

```spl
index=lab_win source="WinEventLog:Security" (EventCode=4720 OR EventCode=4732)
| transaction TargetUserName maxspan=10m startswith=EventCode=4720 endswith=EventCode=4732
| where eventcount > 1
| table _time, host, TargetUserName, SubjectUserName, duration
```

## Field reference

| Field | Source | Meaning |
|---|---|---|
| `EventCode=4720` | Security log | A user account was created |
| `EventCode=4732` | Security log | A member was added to a security-enabled local group |
| `TargetUserName` | 4720 | The account that was created |
| `SubjectUserName` | 4720 | The account that performed the creation |
| `SubjectDomainName` | 4720 | Domain or local machine context of the creator |

## Validation

| Item | Detail |
|---|---|
| Atomic test | `Invoke-AtomicTest T1136.001 -TestNumbers 1` |
| Executed at | `__` |
| Events produced | expect 4720, and 4732 if the test adds the account to Administrators |
| Detection fired | `__` |
| Time to detect | `__` |
| Evidence | `screenshots/02-__-t1136-detection.png` |
| Cleanup | `Invoke-AtomicTest T1136.001 -Cleanup` — confirm the account is gone |

## False positives

| Scenario | Likelihood | Tuning |
|---|---|---|
| Helpdesk creating a local admin during a break-fix | Medium in production, low in this lab | Allowlist known technician accounts; alert on the correlation rather than bare 4720 |
| Software installers creating service accounts | Medium | Allowlist specific account name patterns after observing a baseline |
| Your own lab setup work | High | Filter your build window by time, or baseline after the lab is built |

## Response guidance

1. Identify the creating account and whether it should have that capability at all.
2. Check for a preceding 4688 or Sysmon EID 1 showing `net user` or `New-LocalUser`, and look at the parent process.
3. Determine whether 4732 followed — group membership turns this from suspicious into urgent.
4. Check whether the new account has logged on (4624) and from where.
5. If unexplained: disable the account, preserve the host, and pivot to the originating process tree.

## Gaps

- Account creation through direct LDAP or WMI on the domain controller produces 4720 on DC01, not on the workstation. DC01 must be forwarding for this to have coverage.
- An adversary using an existing dormant account creates no 4720 at all. Pair this with a dormant-account-reactivation detection for real coverage.
- Local account creation with auditing disabled generates nothing — which is why the STIG audit-policy settings and a detection for 1102 (log cleared) matter alongside this.

## NIST 800-53 mapping

| Control | Relevance |
|---|---|
| AC-2 | Account management — detecting accounts created outside the approved process |
| AU-6 | Audit record review, analysis, and reporting |
| SI-4 | System monitoring — detecting indicators of unauthorized activity |
